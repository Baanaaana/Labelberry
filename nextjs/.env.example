# LabelBerry Next.js Frontend Environment Configuration
# Copy this file to .env and update with your actual values

# Database Configuration (same as backend)
# For PostgreSQL (Neon cloud database):
DATABASE_URL="postgresql://username:password@host/database?sslmode=require"
# For local PostgreSQL:
# DATABASE_URL="postgresql://labelberry:password@localhost:5432/labelberry"

# NextAuth Configuration (if using authentication)
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-min-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# API Configuration
# These are evaluated at BUILD TIME for Next.js
# For local development:
NEXT_PUBLIC_API_URL="http://localhost:8080/api"
NEXT_PUBLIC_WS_URL="ws://localhost:8080/api"

# For production with Nginx proxy:
# NEXT_PUBLIC_API_URL="https://labelberry.yourdomain.com/api"
# NEXT_PUBLIC_WS_URL="wss://labelberry.yourdomain.com/api"

# For production with direct access:
# NEXT_PUBLIC_API_URL="http://your-server-ip:8080/api"
# NEXT_PUBLIC_WS_URL="ws://your-server-ip:8080/api"

# Node Environment
NODE_ENV=production

# Optional: Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1